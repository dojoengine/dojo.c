// generated by diplomat-tool
import { DojoError } from "./DojoError.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const FieldElement_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.FieldElement_destroy(ptr);
});

/**
 * Represents a Starknet field element (Felt)
 */
export class FieldElement {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("FieldElement is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            FieldElement_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Creates a new FieldElement from a hexadecimal string
     */
    static newFromHex(hex) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const hexSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, hex)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.FieldElement_new_from_hex(diplomatReceive.buffer, hexSlice.ptr);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new DojoError(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
                throw new globalThis.Error('DojoError: ' + cause.toString(), { cause });
            }
            return new FieldElement(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Creates a new FieldElement from big-endian bytes
     */
    static newFromBytes(bytes) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const bytesSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.slice(wasm, bytes, "u8")));

        const result = wasm.FieldElement_new_from_bytes(bytesSlice.ptr);

        try {
            return new FieldElement(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            functionCleanupArena.free();

        }
    }

    /**
     * Returns the field element as a hexadecimal string
     */
    toHex() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.FieldElement_to_hex(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            write.free();
        }
    }

    /**
     * Returns the field element as bytes (big-endian)
     */
    toBytes(result) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const resultSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.slice(wasm, result, "u8")));
    wasm.FieldElement_to_bytes(this.ffiValue, resultSlice.ptr);

        try {}

        finally {
            functionCleanupArena.free();

        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}